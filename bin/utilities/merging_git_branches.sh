# !/usr/bin/env bash
# script to faciliate updating and merging branches in git
# as I often forget the commands

# need to stage any other changes before merging
git checkout main
git pull origin main
git checkout dev
# git pull 

